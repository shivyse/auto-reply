# Full clipping stack: base bot + local transcription + YouTube auto-upload.
-r requirements.txt

# Local Whisper transcription (free, offline). Skip if you use OPENAI_API_KEY
# or YouTube subtitles instead.
faster-whisper>=1.0.0

# Optional: auto-upload finished clips to YouTube Shorts.
google-api-python-client>=2.0
google-auth-oauthlib>=1.0
